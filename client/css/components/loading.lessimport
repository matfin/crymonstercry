#loading.content {
	padding: 0;
	width: 100%;
	height: 100%;
	
	.display(flex);
	.align-items(center);

	i {
		.flex(1);
		font-size: @fontSizeLarge;
		text-align: center;
		.animation(progress 500ms linear infinite);
	}
}