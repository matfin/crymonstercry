#news {
	overflow: hidden;
	.display(flex);
	.flex-wrap(wrap);
	padding-left: @gridSpacing * 2;
	padding-bottom: @gridSpacing * 4;
}

&.mobile {
	#news {
		padding: 0;

		.post {
			width: 100%;
		}
	}
}