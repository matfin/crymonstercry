/**
 *	Server side routes for dealing with callbacks and hooks.
 *	This is where external content providers call hooks when 
 *	content has been updated.
 */
Router.route('/hooks/instagram', function() {

}, {where: 'server'});